# teste-de-timeout-AngularJS
Teste didático para demonstrar o funcionamento do ciclo de digestão do AngularJS. Ao alterar valores fora do framework. ele compara setTimeout, $apply e $timeout, mostrando quando o digest é necessário, quando é automático e por que $timeout + $apply é redundante.
